diff --git a/lib/client/index.js b/lib/client/index.js
index 4ab317cd6443cc6a774c838fb47725c984fa771e..900bc6b272e99017298d3291a5890fa5e1a86043 100644
--- a/lib/client/index.js
+++ b/lib/client/index.js
@@ -632,6 +632,31 @@ body[data-ds-dark-theme] .re-adapt-panel {
   .re-model-menu { animation: none; }
   .re-effort.is-chibi .re-effort-knob { animation: none; }
 }
+/* Fairy local patch: keep the plugin's slider geometry, but remove the
+   bundled character thumb even when an older localStorage preference enables
+   the legacy chibi class. */
+.re-effort.is-chibi {
+  height: 32px;
+}
+.re-effort.is-chibi .re-effort-knob {
+  left: clamp(14px, var(--re-progress), calc(100% - 14px));
+  width: 28px;
+  height: 28px;
+  border: 1px solid rgba(255,255,255,.94);
+  border-radius: 50%;
+  background-color: #fff;
+  background-image: none;
+  background-repeat: initial;
+  background-position: initial;
+  background-size: initial;
+  box-shadow:
+    0 0 0 2px rgba(92,105,255,.12),
+    0 0 14px rgba(121,82,255,.48),
+    0 2px 7px rgba(0,0,0,.3);
+  filter: none;
+  animation: none;
+  transform-origin: center;
+}
 `;
 
 // src/client/index.tsx
