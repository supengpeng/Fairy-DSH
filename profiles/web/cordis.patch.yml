# Your patch layer for this dsh profile, applied after every bundle layer:
# a top-level YAML array of loader patch entries (id-targeted config
# overrides, disables, and insert lists; `!!js` expressions allowed).
- id: sandbox-policy
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: approval
  config:
    policy: never

- id: permission
  config:
    defaultPreset: danger-full-access

- insert:
    # The MCP host spawns servers with a filtered environment, so the proxy
    # receives its paths as arguments instead of reading DSH_HOME.
    #
    # '@deepseek-ai/dsh-mcp-client' needs no dependency entry here: it ships
    # with the dsh installation, and profile boots resolve installation-owned
    # packages through the healed $DSH_HOME/profiles/node_modules fallback.
    - id: mcp-playwright
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        serverName: playwright
        transport: stdio
        command: !!js process.env.DSH_FAIRY_REPO_ROOT + '/browser-dock/dsh-browser-dock/proxy.cjs'
        args:
          - '--runtime-dir'
          - !!js process.env.DSH_HOME + '/browser-dock'
          - '--playwright-bin'
          - !!js process.env.DSH_FAIRY_PROFILE_ROOT + '/node_modules/@playwright/mcp/cli.js'
          - '--browser'
          - 'chrome'
          - '--headless'
          - '--user-data-dir'
          - !!js process.env.DSH_FAIRY_TEST_HOME + '/playwright-profile'

    - id: mcp-context7
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        serverName: context7
        transport: stdio
        command: !!js process.env.DSH_FAIRY_PROFILE_ROOT + '/node_modules/.bin/context7-mcp'
        args: []

    - id: balance-meter
      name: 'dsh-balance-meter'

    - id: browser-dock
      name: 'dsh-browser-dock'

    - id: fairy-startup
      name: 'dsh-fairy-startup'

    - id: fairy-voice
      name: 'dsh-fairy-voice'

    - id: fairy-visual
      name: 'dsh-fairy-visual'
